import PageShell from './PageShell';

/**
 * Shop / product listing page (PLP). Phase 3 routing scaffold — filters
 * (type/brand/price), sort, the responsive grid, and pagination are built on
 * top of this shell in the ShopPage step.
 */
export default function ShopPage() {
  return <PageShell eyebrow="SHOP" title="All helmets" />;
}
