import PageShell from './PageShell';

/**
 * Cart page. Phase 3 routing scaffold — line items from useCart(), order
 * summary, promo input, empty-cart state, and the checkout CTA are built on
 * top of this shell in the CartPage step.
 */
export default function CartPage() {
  return <PageShell eyebrow="CART" title="Your cart" />;
}
